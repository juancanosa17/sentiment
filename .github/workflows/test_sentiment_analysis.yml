# 📁 .github/workflows/test_sentiment_analysis.yml

# Nombre del workflow que aparecerá en la pestaña "Actions" de GitHub
name: Test de Análisis de Sentimientos

# Define cuándo se debe ejecutar el workflow
on:
  push: # Se activa en cada push a cualquier rama
    branches:
      - main
  pull_request: # También se activa al abrir un Pull Request hacia main
    branches:
      - main

# Define los trabajos (jobs) que se ejecutarán
jobs:
  build-and-test:
    # El tipo de máquina virtual donde se ejecutará el trabajo
    runs-on: ubuntu-latest

    # Los pasos que componen el trabajo
    steps:
      # 1. Clona tu repositorio en la máquina virtual
      - name: Checkout del código
        uses: actions/checkout@v4

      # 2. Configura el entorno de Python en la versión 3.10
      - name: Configurar Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. Instala las dependencias listadas en requirements.txt
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Ejecuta el script de Python para verificar que todo funciona
      - name: Ejecutar script de análisis de sentimientos
        run: python src/sentiment_analyzer.py
